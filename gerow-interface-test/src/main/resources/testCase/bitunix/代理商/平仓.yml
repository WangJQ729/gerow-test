name: 平仓
story: partners
invocationCount: 1
testMethod:
  - name: 平仓
    enable: false
    dataProvider:
      - userId: 13664
    step:
      - keyWord: 通过uid获取用户token
      - keyWord: 持有仓位信息
      - keyWord: 市价平仓
  - name: 取消带单
    dataProvider:
      - userId: 50881
    step:
      - keyWord: 通过uid获取用户token
      - keyWord: 获取子账户Uid
        headers:
          Token: ${token}
      - keyWord: 获取带单子账户token
        iter:
          subAccountUid: ${subAccountUids}
        headers:
          Token: ${token}
      - keyWord: 取消带单
        headers:
          Token: ${token}
