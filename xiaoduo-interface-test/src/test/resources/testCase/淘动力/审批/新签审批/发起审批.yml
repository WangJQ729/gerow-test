name: 发起审批
story: 发起审批
dataProvider:
  - audit_type: deal_new
beforeClass:
  name: 前置条件设置
  step:
    - name: 获取我可创建的审批列表
      url: /api/audit/me/create/list
      method: GET
      assertion: [json: {$.code: 0}]
      extractor:
        - json:
            adudit_id: $.data.list[?(@.audit_type=='${audit_type}')].id
testMethod:
  - name: 参数校验
    description: |
      """</br>
      参数校验
      """
    severity: NORMAL
    step:
      - name: 创建审批

